{
  "name": "cc-tstl-template",
  "version": "1.100.4",
  "description": "Template project for ComputerCraft programs written in TypeScript.",
  "main": "main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tstl",
    "watch": "nodemon --exec \"npm run build && node lib/index.js\" -e js --ignore lib/ || exit 1"
  },
  "devDependencies": {
    "@jackmacwindows/lua-types": "^2.11.0",
    "@jackmacwindows/typescript-to-lua": "^1.4.0",
    "typescript": "^4.6.0"
  },
  "author": "JackMacWindows",
  "license": "MIT",
  "dependencies": {
    "typescript-to-lua": "^1.4.3"
  }
}
